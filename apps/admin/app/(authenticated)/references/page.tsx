import { PlaceholderPage } from "../components/placeholder-page";

export default function ReferencesPage() {
  return (
    <PlaceholderPage
      description="Upload reference documents (PDF, markdown, URL). Chunked, embedded, and used to ground question generation. Lands in Phase 2."
      page="References"
      phase="Phase 2"
    />
  );
}
