import { PlaceholderPage } from "../components/placeholder-page";

export default function ModulesPage() {
  return (
    <PlaceholderPage
      description="Library of question modules. Generation wizard, editor, and variant preview land in Phase 2 once @repo/generator is wired."
      page="Modules"
      phase="Phase 2"
    />
  );
}
